'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {ArrowRight,ScanSearch} from 'lucide-react';
export function AnalyzeInput({compact=false}:{compact?:boolean}){const router=useRouter();const [value,setValue]=useState('');const [error,setError]=useState('');function go(e:React.FormEvent){e.preventDefault();const v=value.trim();if(v.length<32){setError('Enter a valid Solana address or transaction signature.');return}const type=v.length>60?'tx':'wallet';router.push(`/${type}/${encodeURIComponent(v)}`)}return <form onSubmit={go} className="w-full"><div className={`glass flex items-center gap-3 rounded-2xl p-2 ${compact?'':'shadow-glow'}`}><ScanSearch className="ml-3 shrink-0 text-lime" size={20}/><input aria-label="Solana address or signature" value={value} onChange={e=>{setValue(e.target.value);setError('')}} className="min-w-0 flex-1 bg-transparent px-1 py-3 text-sm outline-none placeholder:text-slate-600" placeholder="Paste a Solana wallet, token mint, or transaction hash…"/><button className="focusable flex items-center gap-2 rounded-xl bg-lime px-5 py-3 text-sm font-semibold text-black">Analyze <ArrowRight size={15}/></button></div>{error&&<p className="mt-2 text-sm text-rose-400">{error}</p>}</form>}
