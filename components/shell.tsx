'use client'; import Link from 'next/link'; import {ShieldCheck,Menu,X} from 'lucide-react'; import {useState} from 'react';
const links=[['Analyze','/analyze'],['Launch Check','/launch-check'],['Dashboard','/dashboard'],['Methodology','/methodology']];
export function Logo(){return <Link href="/" className="focusable flex items-center gap-2.5 font-semibold"><span className="grid h-9 w-9 place-items-center rounded-xl border border-lime/30 bg-lime/10 text-lime"><ShieldCheck size={19}/></span><span>ChainGuard <i className="not-italic text-lime">Daily</i></span></Link>}
export function AppShell({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false);return <div className="relative min-h-screen overflow-hidden bg-grid"><div className="pointer-events-none fixed left-1/3 top-[-200px] h-[500px] w-[500px] rounded-full bg-lime/5 blur-[100px]"/><div className="pointer-events-none fixed bottom-0 right-0 h-[420px] w-[420px] rounded-full bg-violet/5 blur-[100px]"/><header className="sticky top-0 z-50 border-b border-white/[.06] bg-ink/80 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><Logo/><nav className="hidden items-center gap-7 md:flex">{links.map(([n,h])=><Link className="text-sm text-slate-400 transition hover:text-white" href={h} key={h}>{n}</Link>)}<Link href="/analyze" className="rounded-lg bg-lime px-4 py-2 text-sm font-semibold text-black">Analyze wallet</Link></nav><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-white/10 px-5 py-4 md:hidden">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} className="block py-3 text-slate-300" href={h} key={h}>{n}</Link>)}</nav>}</header><main>{children}</main><footer className="border-t border-white/[.07]"><div className="mx-auto grid max-w-7xl gap-8 px-5 py-12 md:grid-cols-2"><div><Logo/><p className="mt-4 text-sm text-slate-400">Daily crypto safety and builder-readiness assistant.</p></div><div className="flex flex-wrap gap-5 text-sm text-slate-400 md:justify-end">{links.slice(0,3).map(([n,h])=><Link href={h} key={h}>{n}</Link>)}<a href="mailto:feedback@chainguard.daily">Feedback</a></div><p className="text-xs text-slate-500 md:col-span-2">ChainGuard Daily provides technical safety signals only. Nothing here is financial advice.</p></div></footer></div>}
