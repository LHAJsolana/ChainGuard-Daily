import {ReportHeader} from '@/components/report'; import {GlassCard,Pill} from '@/components/ui'; import {Blocks,MoveRight,ReceiptText} from 'lucide-react';
export default async function Tx({params}:{params:Promise<{signature:string}>}){const {signature}=await params;return <div className="mx-auto min-h-[76vh] max-w-6xl px-5 py-14"><ReportHeader label="Transaction insight" value={signature} type="tx"/><div className="mt-8 rounded-xl border border-violet/20 bg-violet/10 p-4 text-sm text-violet">Detailed transaction decoding is coming soon.</div><div className="mt-5 grid gap-4 md:grid-cols-3">{[['Status','Needs decoding',ReceiptText],['Programs involved','Provider required',Blocks],['Token movements','Provider required',MoveRight]].map(([a,b,I]:any)=><GlassCard className="p-6" key={a}><I className="text-lime"/><p className="mt-7 text-xs text-slate-500">{a}</p><b className="mt-2 block">{b}</b></GlassCard>)}</div><GlassCard className="mt-5 p-8"><Pill tone="purple">Human explanation</Pill><h2 className="mt-5 text-2xl font-semibold">A readable timeline is being prepared.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Future decoding will distinguish programs, instructions, balance changes, and warnings. Until then, verify the signature on Solscan and treat unknown program calls with care.</p></GlassCard></div>}
