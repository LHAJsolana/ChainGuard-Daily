import {GlassCard,Pill} from '@/components/ui'; import {Eye,HelpCircle,Scale,ShieldCheck,Signal,Database} from 'lucide-react';
const blocks=[['What the tool checks','Public on-chain facts such as balances, recent signatures, failure patterns, and available authority states.',Eye],['What it does not know','Ownership, intent, off-chain agreements, private keys, or whether every indexed provider is complete.',HelpCircle],['Why confidence matters','A signal is only as useful as its source and coverage. Reports disclose when evidence is sampled or unavailable.',Signal],['Facts vs risk signals','Observed facts come from data. Risk signals are explainable rules applied to those facts; they are not verdicts.',Scale],['Not financial advice','ChainGuard evaluates technical safety and readiness. It never recommends buying, selling, or holding assets.',ShieldCheck],['Future data providers','Indexed histories, holder concentration, liquidity, decoded instructions, and reputation sources are planned.',Database]];
export default function Methodology(){return <section className="mx-auto max-w-6xl px-5 py-20"><div className="max-w-3xl"><Pill tone="purple">Trust through clarity</Pill><h1 className="mt-6 text-5xl font-semibold tracking-tight">Safety analysis without <span className="text-gradient">false certainty.</span></h1><p className="mt-5 text-lg leading-8 text-slate-400">ChainGuard Daily turns public signals into understandable checks. It shows its work, names its limitations, and leaves the final decision with you.</p></div><div className="mt-14 grid gap-4 md:grid-cols-2">{blocks.map(([a,b,I]:any)=><GlassCard className="p-7" key={a}><I className="text-lime"/><h2 className="mt-7 text-xl font-semibold">{a}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{b}</p></GlassCard>)}</div><div className="mt-8 rounded-2xl border border-amber/20 bg-amber/10 p-6 text-sm leading-6 text-amber">No score means “safe.” A low score only means that the currently implemented heuristics found fewer visible concerns in the available data.</div></section>}
