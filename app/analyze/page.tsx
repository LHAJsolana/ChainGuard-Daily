'use client';
import {useState} from 'react'; import {AnalyzeInput} from '@/components/analyze-input'; import {GlassCard,Pill} from '@/components/ui'; import {WalletCards,Coins,ReceiptText,Wrench} from 'lucide-react';
export default function Analyze(){const [tab,setTab]=useState<'Wallet'|'Token'|'Transaction'>('Wallet');const type=tab==='Wallet'?'wallet':tab==='Token'?'token':'tx';return <section className="mx-auto min-h-[76vh] max-w-4xl px-5 py-24"><div className="text-center"><Pill>Universal scanner</Pill><h1 className="mt-6 text-4xl font-semibold md:text-6xl">Analyze anything <span className="text-gradient">on-chain.</span></h1><p className="mt-4 text-slate-400">Choose the identifier type, then review live public-chain facts and clearly scoped heuristics.</p></div><div className="mt-12 flex justify-center gap-1 overflow-x-auto">{[['Wallet',WalletCards],['Token',Coins],['Transaction',ReceiptText],['Builder Check',Wrench]].map(([n,I]:any)=><button onClick={()=>n==='Builder Check'?location.href='/launch-check':setTab(n)} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm ${tab===n?'bg-white/10 text-white':'text-slate-500'}`} key={n}><I size={15}/>{n}</button>)}</div><p className="mt-5 text-center text-xs text-slate-500">Wallets and token mints use the same address format, so the selected tab determines the report.</p><div className="mt-3"><AnalyzeInput type={type}/></div><GlassCard className="mt-8 p-6"><p className="text-xs font-semibold uppercase tracking-widest text-slate-500">Accepted input</p><div className="mt-4 grid gap-3 md:grid-cols-3">{[['Wallet address','Base58 · 32–44 chars'],['Token mint','Base58 · 32–44 chars'],['Transaction','Base58 signature · 64–90 chars']].map(([a,b])=><div className="rounded-xl bg-black/20 p-4" key={a}><p className="text-xs text-slate-500">{a}</p><code className="mt-2 block text-xs text-slate-300">{b}</code></div>)}</div></GlassCard></section>}
