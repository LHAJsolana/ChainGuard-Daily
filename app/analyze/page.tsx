'use client'; import {useState} from 'react'; import {AnalyzeInput} from '@/components/analyze-input'; import {GlassCard,Pill} from '@/components/ui'; import {WalletCards,Coins,ReceiptText,Wrench} from 'lucide-react';
export default function Analyze(){const [tab,setTab]=useState('Wallet');return <section className="mx-auto min-h-[76vh] max-w-4xl px-5 py-24"><div className="text-center"><Pill>Universal scanner</Pill><h1 className="mt-6 text-4xl font-semibold md:text-6xl">Analyze anything <span className="text-gradient">on-chain.</span></h1><p className="mt-4 text-slate-400">Start with an identifier. We’ll detect the report type and explain available signals.</p></div><div className="mt-12 flex justify-center gap-1 overflow-x-auto">{[['Wallet',WalletCards],['Token',Coins],['Transaction',ReceiptText],['Builder Check',Wrench]].map(([n,I]:any)=><button onClick={()=>n==='Builder Check'?location.href='/launch-check':setTab(n)} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm ${tab===n?'bg-white/10 text-white':'text-slate-500'}`} key={n}><I size={15}/>{n}</button>)}</div><div className="mt-5"><AnalyzeInput/></div><GlassCard className="mt-8 p-6"><p className="text-xs font-semibold uppercase tracking-widest text-slate-500">Try an example</p><div className="mt-4 grid gap-3 md:grid-cols-3">{[['Wallet address','9xQeWvG816bUx9EP...'],['Token mint','So111111111111...'],['Transaction','5KtPn9pQ2MmA7...']].map(([a,b])=><div className="rounded-xl bg-black/20 p-4" key={a}><p className="text-xs text-slate-500">{a}</p><code className="mt-2 block text-xs text-slate-300">{b}</code></div>)}</div></GlassCard></section>}
