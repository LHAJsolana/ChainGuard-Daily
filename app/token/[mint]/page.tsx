import {ReportHeader} from '@/components/report'; import {GlassCard,Pill,RiskGauge,Signal} from '@/components/ui';
export default async function Token({params}:{params:Promise<{mint:string}>}){const {mint}=await params;return <div className="mx-auto max-w-7xl px-5 py-14"><ReportHeader label="Token safety report" value={mint} extra={<div className="flex items-center gap-3"><RiskGauge score={42}/></div>}/><div className="mt-8 grid gap-5 md:grid-cols-2"><GlassCard className="p-7"><h2 className="text-xl font-semibold">Token overview</h2><div className="mt-5 grid grid-cols-2 gap-3">{[['Name','Unknown token'],['Symbol','—'],['Decimals','9'],['Supply','Requires provider'],['Holders','Coming soon'],['Metadata','Needs review']].map(([a,b])=><div className="rounded-xl bg-black/20 p-4" key={a}><p className="text-xs text-slate-500">{a}</p><p className="mt-2 text-sm">{b}</p></div>)}</div></GlassCard><GlassCard className="p-7"><h2 className="text-xl font-semibold">Authority checks</h2><div className="mt-5 space-y-3"><Signal title="Mint authority" status="Needs review">Authority state requires a live RPC response.</Signal><Signal title="Freeze authority" status="Needs review">Unknown status should not be interpreted as revoked.</Signal><Signal title="Metadata mutability" status="Warning">Mutable metadata can change after interaction.</Signal></div></GlassCard></div><GlassCard className="mt-5 p-7"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">Holder & liquidity signals</h2><Pill tone="purple">Coming soon</Pill></div><p className="mt-3 text-sm text-slate-400">Top-holder concentration, liquidity source, DEX availability, and LP risk require an additional indexed data provider.</p></GlassCard><GlassCard className="mt-5 p-7"><h2 className="text-xl font-semibold">Safety explanation</h2><div className="mt-5 grid gap-5 md:grid-cols-4">{[['Observed facts','The submitted mint has a valid report identifier.'],['Warning signs','Authority states are not yet confirmed.'],['Unknowns','Holders, liquidity, and market activity.'],['Suggested next checks','Verify authorities and holder distribution on an explorer.']].map(([a,b])=><div key={a}><p className="text-sm text-lime">{a}</p><p className="mt-2 text-xs leading-5 text-slate-400">{b}</p></div>)}</div></GlassCard></div>}
